import React from "react";

const Loading = () => {
  return (
    <div className="h-screen w-screen text-center justify-center flex flex-col">
      <h1 className="font-bold text-8xl">LOADING...</h1>
      <p>Refresh page if not resolved.</p>
    </div>
  );
};

export default Loading;
